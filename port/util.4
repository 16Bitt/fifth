: LIST
	LAST @
	:LISTLOOP
		DUP WS + .S SPC
		@ DUP
	NZGOTO LISTLOOP
	DROP BS CR
;

: DUMP
	1 STACK-BASE SP WS 2 * - = NZGOTO ENDDUMP
	DROP
	STACK-BASE

	:DUMPLOOP
		DUP @ . SPC
		WS +
		DUP SP WS 2 * - >= ZGOTO DUMPLOOP
	:ENDDUMP
		DROP CR
;

: :[ 1 STATE ! ;
: :] 0 STATE ! ;
